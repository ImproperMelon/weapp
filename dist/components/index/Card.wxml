<block wx:if="{{$taroCompReady}}">
    <view class="container" bindtap="cardClicked" data-e-tap-so="this" data-e-tap-a-a="{{passage.passageId}}"><text class="title">{{passage.title}}</text>
        <image class="cover" mode="scaleToFill" src="{{passage.cover}}"></image><text class="date">{{passage.modifiedTime}}</text>
    </view>
</block>