<block wx:if="{{$taroCompReady}}">
    <view>
        <card compid="{{$compid__55}}"></card>
        <banner compid="{{$compid__56}}"></banner>
        <block>
            <block wx:if="{{passageList.length > 0}}">
                <card-list compid="{{$compid__57}}"></card-list>
            </block>
            <block wx:else>
                <empty></empty>
            </block>
        </block>
        <banner compid="{{$compid__58}}"></banner>
        <block>
            <block wx:if="{{passageList.length > 0}}">
                <card-list compid="{{$compid__59}}"></card-list>
            </block>
            <block wx:else>
                <empty></empty>
            </block>
        </block>
    </view>
</block>